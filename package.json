{
  "name": "hide-n-seek-game",
  "version": "1.0.0",
  "scripts": {
    "dev": "http-server -p 8080",
    "build": "node deploy-setup.js",
    "deploy": "npm run build && npx gh-pages@5.0.0 -d dist",
    "deploy:manual": "npm run build && cd dist && git init && git add . && git commit -m 'Deploy' && git push -f https://github.com/YOUR_USERNAME/YOUR_REPO.git main:gh-pages",
    "setup-deploy": "npm install gh-pages@5.0.0 && npm run build"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "http-server": "^14.1.1"
  }
}